# Flatpak build artifacts (regenerated by flatpak-builder; never committed)
/build-dir/
/repo/
/.flatpak-builder/

# CMake / build output (in case of a non-Flatpak local build)
/build/
*.o
*.obj

# Editor / OS cruft
*~
.DS_Store

# Kept local; not part of the published repo.
IDEAS.md

# NOTE: data/screenshots/*.png ARE committed on purpose. The AppStream metainfo
# references them via raw.githubusercontent.com URLs, so they must live in the
# repo and be served from the tagged release.
